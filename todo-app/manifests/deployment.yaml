apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: image-cache
          persistentVolumeClaim:
            claimName: todo-image-claim
      containers:
        - name: todo-app
          image: dwk-todo-app:1.12
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3000"
            - name: IMAGE_FILE
              value: /usr/src/app/files/image.jpg
            - name: IMAGE_CACHE_TTL_MS
              value: "600000"
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: image-cache
              mountPath: /usr/src/app/files

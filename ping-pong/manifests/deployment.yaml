apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-pong-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ping-pong
  template:
    metadata:
      labels:
        app: ping-pong
    spec:
      volumes:
        - name: shared-data
          persistentVolumeClaim:
            claimName: shared-log-ping-pong-claim
      containers:
        - name: ping-pong
          image: dwk-ping-pong:1.11
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3000"
            - name: COUNTER_FILE
              value: /usr/src/app/files/ping-pong.txt
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: shared-data
              mountPath: /usr/src/app/files
